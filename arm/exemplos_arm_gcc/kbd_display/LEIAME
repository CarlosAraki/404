Programa que mostra o uso de dispositivos (teclado, mostrador de sete segmentos) com um programa em C.

Para compilar o programa, use digite o comando:

make

ou então execute diretamente:

arm-none-eabi-gcc -o test test.c ../libc/startup.o ../libc/libnosys_gnu.o -lm -nostartfiles -static

obtendo o arquivo executável de nome "test".

Então inicie o simulador com a opção -c (para instanciar a console):

armsim -c -l test

No simulador, execute "g _start".
